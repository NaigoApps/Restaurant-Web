import {ACT_BEGIN_DINING_TABLE_CREATION} from "../../actions/ActionTypes";
import {iSet} from "../../utils/Utils";

class DiningTableEditorHandler{
    handleAction(data, action){
        switch (action.type){

        }
    }
}

const diningTableEditorHandler = new DiningTableEditorHandler();
export default diningTableEditorHandler;